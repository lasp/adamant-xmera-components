---
description: This is a unit test suite for the Hill Point component
tests:
  - name: Test
    description: Run the algorithm to ensure the Ada to C to C++ integration is sound.
  - name: Test_Invalid_Data_Dependency
    description: Ensure a data dependency with the wrong identifier is treated as a wiring defect and fails the tick's assertion.
