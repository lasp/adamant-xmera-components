---
description: Parameters for the Celestial Two Body Point component.
parameters:
  - name: Alignment_Threshold
    description: "[rad] Angle between the directions to the two bodies below which they count as aligned and the orbit normal about the primary body replaces the secondary body. Must lie between 1e-6 and pi/2."
    type: Packed_F32.T
    default: "(Value => 0.017453292)"
